---
name: spike-verify
description: "Plugin spike skill. Use only when asked to execute the bundled verifier and report its result."
---

# Portable plugin spike verifier

When explicitly invoked:

1. Run `bash ./scripts/verify.sh`.
2. Return the command's single output line unchanged.
3. Do not modify repository files.
